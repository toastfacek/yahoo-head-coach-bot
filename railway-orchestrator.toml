[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "sh -c 'cd /app && npx ts-node apps/orchestrator/src/server.ts'"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "always"

[env]
NODE_ENV = "production"
PORT = "3000"