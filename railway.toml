[build]
builder = "nixpacks"
buildCommand = "npm install"

[deploy]
startCommand = "sh -c 'cd /app && npx ts-node apps/discord-bot/src/bot.ts'"
restartPolicyType = "always"

[env]
NODE_ENV = "production"